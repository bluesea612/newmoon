// 跑馬燈配置物件，支援多條訊息輪播
const marqueeConfig = {
    messages: [
        {
            text: "🌊 歡迎體驗桃花島月光藍海！立即預訂享早鳥優惠，平日連住兩晚9折！詳情請洽詢。🌟",
            color: "#0b2545", // 深藍色（與 --dark 一致）
            speed: 30, // 單條訊息滾動速度（秒）
            background: "#e6f0ff" // 淡藍色背景
        },
        {
            text: "🎉 生日當月入住，加贈精緻甜點與飲品，浪漫假期等你來！",
            color: "#d81b60", // 鮮豔粉紅色
            speed: 30,
            background: "#e6f0ff"
        },
        {
            text: "🌅 桃花島月光海奇景，晴朗夜晚最佳觀賞時機，攝影行程報名中！",
            color: "#2e7d32", // 深綠色
            speed: 30,
            background: "#e6f0ff"
        }
    ],
    cycleInterval: 10 // 訊息輪播間隔（秒）
};

/* 使用說明：
1. 修改訊息內容：
   - 在 `messages` 陣列中新增、刪除或修改物件。
   - 每個物件包含：
     - `text`：跑馬燈顯示的文字（支援表情符號，如 🌊、🌟）。
     - `color`：文字顏色，使用高對比度 CSS 顏色值（例如 "#0b2545", "#d81b60", "#2e7d32"），確保在背景 #e6f0ff 上清晰可讀（對比度符合 WCAG 4.5:1）。
     - `speed`：單條訊息從容器中段滾動到完全移出左側的時間（單位：秒），建議 20-60 之間。
     - `background`：跑馬燈容器背景顏色（例如 "#e6f0ff"）。
   - 示例：
     {
         text: "新訊息內容",
         color: "#ff0000",
         speed: 25,
         background: "#ffffff"
     }

2. 修改輪播間隔：
   - `cycleInterval`：控制每條訊息顯示多久後自動切換（單位：秒），建議 5-15 秒。
   - 若只有一條訊息，`cycleInterval` 無效，訊息將持續滾動。

3. 跑馬燈行為：
   - 訊息從容器中段開始（50% 位置），滾動到文字完全移出容器左側後切換下一則。
   - 手機端確保訊息完整顯示後才切換，解決窄螢幕閱讀問題。
   - 支援「暫停」(⏸)、「繼續」(▶)、「上一則」(◄)、「下一則」(►) 按鈕。
   - 手動切換（上一則/下一則）會暫停自動輪播，繼續播放時重新計時。

4. 注意事項：
   - 確保至少有一條訊息（`messages` 陣列不為空）。
   - 修改後儲存檔案，重新載入網頁即可生效。
   - 若要禁用輪播，設 `messages` 為單一物件陣列，或將 `cycleInterval` 設為大值（如 9999）。

5. 進階調整：
   - 調整跑馬燈高度或字體大小：修改 `.marquee-container` 和 `.marquee-text` 的 CSS。
   - 調整按鈕樣式或位置：修改 `.marquee-controls` 和 `.marquee-controls button` 的 CSS。
*/